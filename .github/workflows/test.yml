name: Secret Test

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Show Secret
      run: echo "Secret password is ${{ secrets.EMAIL_ADDRESS }}"
